apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.payment.name }}
  namespace: {{ .Values.namespaces.application }}
  labels:
    app: payment
spec:
  selector:
    app: payment-microservice
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP