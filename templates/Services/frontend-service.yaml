apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.frontend.name }}
  namespace: {{ .Values.namespaces.application }}
  labels:
    app: sales-ui 
spec:
  type: ClusterIP
  selector:
    app: sales-ui
  ports:
    - port: 8000
      targetPort: 3000
      protocol: TCP